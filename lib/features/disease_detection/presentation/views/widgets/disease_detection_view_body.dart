import 'package:flutter/cupertino.dart';

class DiseaseDetectionViewBody extends StatelessWidget {
  const DiseaseDetectionViewBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        
      ],
    );
  }
}
